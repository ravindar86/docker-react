sudo: required

language: generic

services:
  - docker

before_install:
  - docker build -t ravindar/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ravindar86/docker-react npm run test
